<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/compra.css">
    <title>Finalizar Compra - TechTweaks</title>
</head>

<body>
    <header>
        <div class="navbar-brand">
            <span class="navbar-logo">
                <a href="/">
                    <img src="assets/images/logo-4-121x121.png" alt="Mobirise" style="height: 4.1rem;">
                </a>
            </span>
        </div>
        <nav>
            <a href="/">Home</a>
            <a href="servicos">Serviços</a>
            <a href="login">Login</a>
            <a href="contato">Contato</a>
            <a href="carrinho">
                <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none"
                    stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                    class="feather feather-shopping-cart">
                    <circle cx="9" cy="21" r="1"></circle>
                    <circle cx="20" cy="21" r="1"></circle>
                    <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
                </svg>
            </a>
        </nav>
    </header>
    <main>
        <h2>Finalizar Compra</h2>
        <section class="checkout">
            <h3>Resumo do Pedido</h3>
            <div class="order-summary">
                <p><strong>1x Otimização de Sistema - </strong> R$ 150,00</p>
                <p><strong>1x Formatação Completa</strong> R$ 200,00</p>
                <p><strong>Total:</strong> R$ 350,00</p>
            </div>

            <h3>Informações de Pagamento</h3>
            <form>
                <div class="form-group">
                    <label for="payment-method">Método de Pagamento</label>
                    <select id="payment-method" name="payment-method" required>
                        <option value="cartao">Cartão de Crédito</option>
                        <option value="pix">PIX</option>
                        <option value="boleto">Boleto Bancário</option>
                    </select>
                </div>

                <div id="cartao-info" class="payment-info">
                    <div class="form-group">
                        <label for="card-number">Número do Cartão</label>
                        <input type="text" id="card-number" name="card-number">
                    </div>
                    <div class="form-group">
                        <label for="card-name">Nome no Cartão</label>
                        <input type="text" id="card-name" name="card-name">
                    </div>
                    <div class="form-group">
                        <label for="card-expiry">Validade (MM/AA)</label>
                        <input type="text" id="card-expiry" name="card-expiry">
                    </div>
                    <div class="form-group">
                        <label for="card-cvv">CVV</label>
                        <input type="text" id="card-cvv" name="card-cvv">
                    </div>
                </div>

                <div id="pix-info" class="payment-info" style="display: none;">
                    <p>Após finalizar a compra, o código QR para pagamento via PIX será gerado.</p>
                </div>

                <div id="boleto-info" class="payment-info" style="display: none;">
                    <p>Após finalizar a compra, o boleto bancário será gerado.</p>
                </div>

                <button type="submit" class="btn">Finalizar Compra</button>
            </form>
        </section>
    </main>
    <footer>
        <nav>
            <a href="sobre">Sobre Nós</a>
            <a href="servicos">Serviços</a>
            <a href="contato">Contato</a>
        </nav>
        <p>&copy; 2024 TechTweaks. Todos os direitos reservados.</p>
    </footer>

    <script>
        const paymentMethod = document.getElementById('payment-method');
        const cardInfo = document.getElementById('cartao-info');
        const pixInfo = document.getElementById('pix-info');
        const boletoInfo = document.getElementById('boleto-info');

        paymentMethod.addEventListener('change', function () {
            cardInfo.style.display = 'none';
            pixInfo.style.display = 'none';
            boletoInfo.style.display = 'none';

            if (this.value === 'cartao') {
                cardInfo.style.display = 'block';
            } else if (this.value === 'pix') {
                pixInfo.style.display = 'block';
            } else if (this.value === 'boleto') {
                boletoInfo.style.display = 'block';
            }
        });
    </script>
</body>

</html>